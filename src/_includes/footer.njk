    <!-- Footer -->
    <footer class="footer footer-center p-10 bg-base-300 text-base-content">
        <aside>
            <p class="font-bold text-lg">3DScanPost</p>
            <p>Professional 3D scanning service for hobbyists and small businesses</p>
            <p class="opacity-70">NH based service. Scan data provided in STL and PLY formats.</p>
        </aside>
        <nav>
            <p>&copy; 2026 3DScanPost. All rights reserved.</p>
            <p>Part of <a href="https://jalapeno.design">Jalapeno Design</a>
        </nav>
    </footer>

    <script>
        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    const offset = 80;
                    const targetPosition = target.offsetTop - offset;
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });
    </script>
